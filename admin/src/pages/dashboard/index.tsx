import React from 'react'; import { Card, Typography } from 'antd'; export default function Dashboard(){return(<div style={{padding:24}}><Card><Typography.Title level={3} style={{marginTop:0}}>Dashboard</Typography.Title><p>Dashboard page</p></Card></div>)};
